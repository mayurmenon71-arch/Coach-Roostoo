# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env / secrets — never commit provider keys (set them in the Vercel dashboard)
.env
.env.*

# OS / editor
.DS_Store

# Local tooling
.claude/

# RAG — the vector index is a build artifact, rebuilt from the cards
chroma_db/
